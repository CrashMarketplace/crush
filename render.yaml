services:
  - type: web
    name: crush
    env: node
    buildCommand: cd server && npm ci && npm run build && cd ../client && npm ci && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000

